export enum Station {
  APP_BOOTHS = 'APP_BOOTHS',
  COUNTERS = 'COUNTERS',
  ATMS = 'ATMS',
  ATM_COINS = 'ATM_COINS',
  VTMS = 'VTMS'
}

export enum StationEquipmentCount {
  APP_BOOTHS = 2,
  COUNTERS = 11,
  ATMS = 6,
  ATM_COINS = 1,
  VTMS = 3
}

export enum StationStatus {
  OCCUPIED = 'OCCUPIED',
  VACANT = 'VACANT'
}
